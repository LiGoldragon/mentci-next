# Agent instructions

Cross-project rules (jj workflow, always-push): see `~/git/tools-documentation/AGENTS.md`.

Curated tool references (bd, jj, dolt, nix) live in `~/git/tools-documentation/`. Write discoveries there as topic files.

`bd` (beads) tracks issues. `bd prime` auto-runs at session start and gives current state.
