# 071 — cli-protocol policies + implementation order

*Claude Opus 4.7 · 2026-04-25 · synthesis of two parallel
research agents (cli-protocol refinement + implementation
order with client-msg insight) plus the wire-format decision Li
prompted on prefix encoding. The client-msg types are now
skeleton-as-design at [`repos/nexusd/src/client_msg/`](repos/nexusd/src/client_msg/);
this report carries the policy detail and lays out next-work
sequencing.*

---

## 1 · Where this fits

State at the start of this round: language design + criome-msg
contract sketched in [reports/070](repos/mentci-next/reports/070-nexus-language-and-contract.md);
two blocking Li questions (Q-α kind set, Q-β genesis principal)
in [reports/067](repos/mentci-next/reports/067-what-to-implement-next.md);
no client-msg layer.

Three things landed during this round:

1. **client-msg as a protocol layer.** Separate from criome-msg.
   Any client (nexus-cli, editor LSP, scripts, agents, future
   tools) ↔ nexusd; criomed never sees client-side `RequestId`.
   Skeleton at [`repos/nexusd/src/client_msg/`](repos/nexusd/src/client_msg/);
   nexusd is `[lib] + [[bin]]` so clients import the contract
   types from the library half.
2. **Wire format committed.** Both client-msg and criome-msg
   wires carry a stream of frame archives. Both parties know the
   frame schema; framing is intrinsic to it; no raw-byte primitive
   layer outside rkyv. Per Li 2026-04-25: "rkyv schema is known
   by both parties; we just need a universal handshake message."
3. **client-msg variants.** `Request::{Send, Heartbeat, Cancel,
   Resume}`; `Reply::{Ack, Working, Done, DoneWithFallback,
   Failed, Cancelled, ResumedReply, ResumeNotReady,
   FailedFallback}`. `FallbackSpec { path, format }`,
   `FallbackFormat::{Nexus, Rkyv}`.

---

## 2 · client-msg policies

The skeleton commits to the *wire shape*. Policies (timing,
file paths, ID strategy, state machine) live here — easier to
tune as implementation lands.

### 2.1 · Heartbeat

- **Interval default** (nexus-cli's policy; other clients may
  pick differently): 5 seconds. nexus-cli sends the first
  heartbeat 5s after a `Send` if no reply has arrived; subsequent
  intervals exponentially back off (5 → 10 → 20 → 60s, capped).
- **Liveness threshold**: two consecutive heartbeats without
  reply ⇒ assume socket is dead.
- **On dead socket**: nexus-cli attempts one reconnect; if that
  fails, falls back to polling the fallback file (if the client
  supplied one). If neither works, exit with `daemon
  unreachable, fallback unavailable`.
- **Heartbeat for unknown `RequestId`** (nexusd has no
  record — process restart, connection reset): reply
  `Reply::Failed { error: "request id not found; socket may
  have been reset" }`. Client interprets: original lost; use
  Resume if fallback exists, or retry from scratch.
- **nexusd does not forward heartbeats to criomed.** Heartbeat
  state is local; nexusd answers from its own in-memory
  `RequestState`. Criomed never sees these probes.

### 2.2 · Fallback file

- **Path**: client-supplied (`FallbackSpec::path`). No nexusd
  default — the client decides where state goes; nexusd never
  surprises with file writes outside the supplied path.
- **Atomic write**: `${path}.in-progress` is written, then
  `rename(temp, path)`. Polling clients never observe a
  half-written file.
- **Mode**: 0600 (owner-only). Replies may carry sensitive
  diagnostics or intermediate state.
- **Format default at rung 1**: `FallbackFormat::Nexus` (human-
  readable text). `Rkyv` is available for tools that prefer the
  canonical binary form. Client picks per request.
- **Cleanup**: on successful `Resume`, nexusd reads the file
  and deletes it. Orphaned files (client never resumed) are
  the client's TTL/cleanup responsibility (e.g., `find
  ${fallback_dir} -mtime +7 -delete`).
- **Concurrent resumes**: first `Resume` to acquire the file
  wins (read + delete is atomic); the second gets
  `ResumeNotReady`.

### 2.3 · `RequestId` strategy

- **Format**: UUID v7 (time-ordered, globally unique). 16
  bytes. Generated by the client; opaque to nexusd; never seen
  by criomed.
- **Why UUID v7**: collision-free across processes (multiple
  clients sharing a fallback dir don't step on each other),
  time-ordered (logging stays sortable), unguessable (logs in
  bug reports don't leak structure).
- The skeleton uses `pub struct RequestId(pub u128)` — 16-byte
  UUID-shaped slot. Implementation fills via `uuid::Uuid::v7`
  (or equivalent) cast to `u128`. Resolved at skeleton
  landing.

### 2.4 · nexusd state machine (per in-flight request)

| State | Entry | Transitions | Persistence |
|---|---|---|---|
| Idle | connected; no Send outstanding | `Send` → Parsing | memory |
| Parsing | `Send` received | parse ok → BuildingMsg; parse fail → Failed | memory |
| BuildingMsg | AST ready | build ok → AwaitingCriomed; build fail → Failed | memory |
| AwaitingCriomed | criome-msg sent to criomed; reply pending | criomed reply → Serialising; client socket drop → WriteFallback | memory + fallback path (if supplied) |
| Serialising | criomed reply received; encoding nexus text | encode ok → Done; encode fail → Failed | memory |
| WriteFallback | client socket dropped mid-flight | atomic write ok → FallbackWritten; write fail → FailedFallback | **persistent** (file on disk) |
| Done | reply ready or fallback written | reply sent → Idle; or cleanup → Idle | memory |
| Failed | parse / build / criomed-transport error | error sent → Idle | memory (no fallback) |
| FailedFallback | fallback write failed | error sent → Idle | memory |

Persistence boundary: only `FallbackWritten` survives nexusd
restart. Heartbeats for in-memory state during `Awaiting…`
return `Working { stage: AwaitingCriomed }`.

### 2.5 · Cancel semantics

- Cancel for already-replied request → `Cancelled` (no-op
  acknowledgement).
- Cancel for `AwaitingCriomed` → nexusd discards the eventual
  criomed reply, replies `Cancelled` immediately. Criomed is
  not asked to cancel (criome-msg v0.0.1 has no cancel verb;
  added if real demand surfaces).
- Cancel for unknown request_id → `Failed { error: "request id
  not found" }`.

### 2.6 · Connection model

- Single socket path: `${XDG_RUNTIME_DIR:-/tmp}/nexusd.sock`
  with optional symlink at `~/.nexus/nexusd.sock`. The same
  path serves all clients (CLI, LSPs, scripts, agents).
- One client invocation = one connection (typical). Multiple
  in-flight requests on one connection are allowed.
- nexusd-side correlation: `HashMap<(connection_id,
  RequestId), RequestState>`. Per-connection isolation is the
  OS's job.

---

## 3 · Wire format — the frame schema is the framing

Both client-msg and criome-msg wires carry a stream of frame
archives — `Archived<Frame>` and `Archived<Frame>`
respectively. Both parties know the schema, so framing is
intrinsic to it: the universal handshake message is the frame
type itself. No "raw bytes outside rkyv" layer; the schema
encodes whatever structure delimiters the implementation
needs.

reports/070 §6 updated to match.

---

## 4 · Implementation order

The skeleton-as-design pattern dominates the next phase. Bodies
land afterwards along the narrowest path to rung-1 success
criterion (per [reports/064 §2.2](repos/mentci-next/reports/064-bootstrap-as-iterative-competence.md)).

### 4.1 · Inventory of landable skeleton

| # | Target | Scope | Depends on | Est. LoC |
|---|---|---|---|---|
| 1 | `criome-msg/` (CREATE) | All of `Frame`, `AuthProof`, `Request` enum, `Reply` enum, `Selection`/`RawPattern`/`RawOp`/`RawProjection`, `Diagnostic` family, `RawRecord`/`RawValue` per [reports/070 §6](repos/mentci-next/reports/070-nexus-language-and-contract.md) | none (Slot/Revision/Blake3Hash newtypes embed locally) | 600–900 |
| 2 | `criome-schema/` (CREATE) | ~15-kind v0.0.1 set per [reports/067 §2 Q-α](repos/mentci-next/reports/067-what-to-implement-next.md) | Q-α answer | 400–600 |
| 3 | `sema/` scaffold (replace stub) | redb tables; `SemaWrite` trait; bodies `todo!()` per [reports/064 §2.4 step 3](repos/mentci-next/reports/064-bootstrap-as-iterative-competence.md) | criome-schema | 300–500 |
| 4 | `criomed/` (CREATE) | boot loop; six-step validator pipeline trait; genesis dispatcher; criome-msg listener; lojix-msg client (unwired) | crates 1, 2, 3 + Q-β | 500–800 |
| 5 | `nexusd/` extension | `criome_msg/` mod parallel to existing `client_msg/`; criomed UDS client; client-msg → criome-msg translator | crate 1 | 200–400 |
| 6 | `nexus-cli/` extension | path-dep on nexusd-the-library; UDS connector; argv/stdin input; reply printer; heartbeat + resume | nexusd::client_msg | 150–300 |
| 7 | `criomed/genesis.nexus` | ~30 line nexus text per [reports/067 §3.2](repos/mentci-next/reports/067-what-to-implement-next.md) | crate 2 + Q-β | 30–50 |

Total: ~2200–3500 LoC of compiled skeleton + bodies-`todo!()`.

### 4.2 · Sequencing

```
Step 0: Li answers Q-α (kind set) + Q-β (genesis principal mechanism).
─────────────────────────────────────────────────────────────────────
Step 1:  criome-msg scaffold                              ~700 LoC
Step 2:  criome-schema scaffold        (needs Q-α)         ~500 LoC
Step 3:  sema scaffold                                     ~400 LoC
                                                           ─────
Steps 1-3 parallel.

Step 4:  criomed scaffold              (needs 1,2,3 + Q-β) ~600 LoC
Step 5:  nexusd::criome_msg + client   (needs 1)           ~300 LoC
Step 6:  nexus-cli wires client-msg                           ~200 LoC
                                                           ─────
Steps 4-6 parallel.

Step 7:  genesis.nexus text            (needs 2 + Q-β)     ~30 LoC

Step 8:  cargo check --workspace passes everywhere.
         No runtime; no smoke test yet.
         ← rung-1 DESIGN milestone.

Step 9+: bodies along the narrowest path to rung-1 success
         criterion (per reports/064 §2.2).
```

Splitting design-lock from implementation matches the
architecture rule "skeleton-as-design over prose-as-design"
and the [`repos/lojix-store/src/`](repos/lojix-store/src/)
exemplar.

### 4.3 · Genesis self-loop resolution

At first boot, criomed needs to dispatch `genesis.nexus`. Two
candidate architectures:

(a) criomed reads the file, parses it via `nota-serde-core`
in-process, builds criome-msg envelopes, writes records to sema
directly. **Wrong** — bypasses nexusd; reintroduces the "two
journeys" hand-wave [reports/065 §2](repos/mentci-next/reports/065-criome-schema-design.md)
catalogued as wrong-framing. Even though the resulting bytes
match what nexusd would produce, the path violates the rung-by-
rung rule.

(b) criomed reads the file (`include_str!`), opens a UDS
client to nexusd, sends each `(Assert ...)` as a
`Request::Send`. nexusd parses, builds criome-msg, sends
*back* to criomed via the criome-msg socket. criomed validates
and writes. **Correct** — the genesis stream is a stream like
any other; nexusd's role is honoured; multi-process from day
one holds.

The "self-loop" objection is conceptual not real — it's a
Unix-socket loop, not a control-flow loop. Tokio handles it
trivially.

This makes the "built-in Rust types" usage in criomed's
schema-check step a *fallback during the genesis stream*
(when no in-sema KindDecl exists yet for the kind being
asserted), not a primary input path. Mechanism stays
explainable.

---

## 5 · Implement-vs-design split

**Skeleton-as-design dominates this phase.** No more pure
prose reports until the skeleton compiles end-to-end.

- Real implementation is appropriate for tightly-bounded leaf
  functions whose shape is unambiguous: `RequestId::fresh()`
  (uuid v7), `Frame::encode`/`decode` (rkyv API call), redb
  table opens.
- Real implementation is *premature* for criome-msg's verb
  handlers, criomed's validator pipeline steps, nexusd's cli-
  msg listener — interfaces are still adjusting; locking
  rkyv-derive-checked types catches drift faster than locking
  bodies.
- After the cargo-check-workspace-passes milestone (Step 8),
  the next phase fills bodies along the narrowest path to the
  Stage A success criterion.

Mix recommendation: **80% skeleton-as-design + 15% trivially-
bounded real bodies + 5% genesis.nexus text + 0% new prose
reports**.

---

## 6 · Carried-over Li questions

These remain blocking. Confirming or revising them unblocks
Steps 1–7.

### Q1 (was 067 Q1, now Q-α) · ~15-kind v0.0.1 set

Listed in [reports/067 §2 Q-α](repos/mentci-next/reports/067-what-to-implement-next.md):

- Schema-of-schema: `KindDecl`, `FieldSpec`, `TypeRef`,
  `VariantDecl`, `CategoryDecl`
- Slot/index: `Slot`, `Revision`, `SlotBinding`, `SemaGenesis`
- Audit: `ChangeLogEntry`
- Authz (degenerate at rung 1): `Principal`, `Quorum`,
  `Policy`
- Supporting literals: `LiteralValue`, `PrimitiveType`,
  `CollectionKind`, `StabilityTag`, `FieldVisibility`,
  `ChangeOp`, `Op`

Confirm or revise.

### Q2 (was 067 Q2, now Q-β) · Genesis principal mechanism

(a) hardcoded `bootstrap_principal_id` constant in criomed
binary, used for every assertion in the genesis stream;
retired the moment `SemaGenesis` lands. Or (b) first message
in `genesis.nexus` is `(Assert (Principal …))` and bypasses
permission-check; subsequent messages claim that Principal.

Research-agent lean: (a) — names the special case clearly,
keeps validator state-free during genesis. Confirm or
override.

---

## 7 · Three new questions for Li

### Q3 · `Slot` / `Revision` / `Blake3Hash` newtype location

These newtypes appear in `criome-msg` (wire), `criome-schema`
(record fields), `sema` (table keys). Three candidates:

(i) Duplicate per crate (each derives rkyv separately).
(ii) Define in `criome-schema`; re-export from `criome-msg`
and `sema`.
(iii) Define in a tiny `criome-types` crate that all three
depend on.

Lean: (iii) for cycle-safety and dependency-graph cleanliness.
Confirm.

### Q4 · Cancel-criomed verb — add to criome-msg or skip?

client-msg has a `Cancel` verb; nexusd handles it locally by
discarding the criomed reply. If real workflows need to abort
expensive criomed operations (long-running queries, complex
cascade evaluations), criome-msg would need a parallel cancel
verb. Add now (for future-proofing) or wait until demand
surfaces?

---

## 8 · Open questions still on the books

Not blocking but informing later work. Listed for visibility:

- 070 §8 Q1 (subscription delivery semantics)
- 070 §8 Q3 (intra-txn forward-refs)
- 070 §8 Q4 (cascade non-termination → diagnostic)
- 070 §8 Q5 (criome-msg vs criome-net split)
- 070 §8 Q6 (ExecutionPlan granularity)
- 065 §6 Q1 (`nexus-schema` rename to `machina-schema`)
- 065 §6 Q3 (Invariant kind vs Rule with `is_must_hold`)
- 065 §6 Q4 (AuditEntry materialised vs derived)
- 065 §6 Q5 (`ConstraintDecl::Custom` extensibility)
- 065 §6 Q6 (stratum contiguity in `CategoryDecl`)

Each becomes near-term when its corresponding rung is.

---

*End report 071.*
